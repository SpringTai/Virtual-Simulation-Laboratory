# 2.1 改造验收记录

2026-09-13。Python 平台层 + C++ 原生核心的第一阶段版本。

写实动画比例修正：按各实验自身标准试样等比例绘制，长径比分别为拉伸4、压缩2、扭转12、弯曲24、剪切2、压杆失稳60。同一实验修改尺寸继续复用同一参考动画。新增三个视口尺寸的长径比检查，并重新完成72个显示状态检查及压杆/压缩截图检查。

- Windows 原生 Zig/Clang 编译完成，DLL ABI 版本1，仅依赖 Windows 系统运行库；未使用 WSL。
- 16组后端对照通过：矩形 Fe/Al 拉伸/压缩完整历程、两截面两材料的扭转/弯曲/失稳。包括多个网格规模的组装矩阵比较。
- 原圆棒核心 solver.py 与历史发布源码 SHA256 相同。完整回归中最大反力差约1.64×10⁻⁶ N，为峰值的4.22×10⁻¹¹；坐标差约1.31×10⁻¹² mm，断裂时刻及失效单元一致。原有近零反力的绝对容差改为峰值归一化容差1×10⁻¹⁰，回归通过。
- 结构独立理论校核16组通过，最大相对误差约8.45×10⁻⁷。完整轴向验证脚本通过。
- 6种实验、2种显示风格、2种分辨率、3个播放位置，共72个界面状态通过；中文字体及界面截图已检查。
- 原生库随EXE打包。冻结程序完成24份标准回放、12组独立计算、NPZ往返及CSV/PNG导出检查。
- 在含空格的自定义安装路径下完成同样自检；更新保留用户文件，卸载只清除安装文件，测试用户文件保留；拒绝无关非空目录。
- 安装验证使用不写快捷方式/注册表的测试选项，未改动用户正在使用的桌面入口。文件夹选择对话框已实现，实际安装目录复制、更新及卸载已验证。

性能边界：当前矩形轴向小模型的 C++ 实现尚慢于已预热的 Numba，结构组装在这轮单次测量中部分更快；没有宣称整体加速。详见 verification-output/v2.1/native-parity.json。下一阶段可按大型模型剖析结果优化。

视觉边界：写实模式是程序绘制的2.5D教学动画，按同类实验复用，不是计算预测的裂纹或AI视频。网格模式保留真实计算场。未添加混凝土或土力学实验。

交付物：release-v2.1，以及 MechanicsVirtualLab-v2.1-Windows-x64-offline.zip。完整验收数据在 verification-output/v2.1，ZIP逐文件一致性记录在 release-validation.json。
