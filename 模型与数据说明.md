# 模型与数据说明

该程序面向材料力学核心概念与后续 Python 数据处理教学。六类实验都支持圆形和矩形截面，使用 Fe、Al 两组代表参数。预计算是将相同输入的求解结果随程序保存，供快速回放；不涉及机器学习训练。手动尺寸会进入计算模型，结果不由预设曲线缩放生成。

## 参数与单位

长度 mm，力 N，正应力/剪应力/弹性模量 MPa，弯矩/扭矩 N·mm，转角 rad，应变无量纲。Fe：E=200000 MPa，ν=0.30，初始屈服250 MPa；Al：E=70000 MPa，ν=0.33，初始屈服120 MPa。它们是教学代表参数，不是纯铁/纯铝或某一牌号的标定试验数据。

圆形 A=πd²/4，弯曲 I=πd⁴/64，扭转 J=πd⁴/32。矩形 A=bh，绕所选弯曲轴 I=bh³/12；失稳取弱轴惯性矩。矩形 Saint-Venant 扭转常数由相应级数计算，不能以极惯性矩代替。

## 拉伸

圆棒：四节点轴对称实体单元，2×2 高斯积分，完整 2πR 体积权重；有限变形对数应变 J2 塑性、Voce 强化与渐进损伤。只约束端面轴向位移，半径方向自由，轴线上径向对称。中部半径微小初始缺陷用于选择颈缩位置。

矩形：有限变形非线性一维杆单元，初始面积取真实 b×h。宽和高根据轴向伸长、塑性体积约束及弹性泊松效应更新。轴力平衡、局部变细和损伤按该降维模型计算；它不解析矩形颈部的三维应力分布。

Voce 强化采用 σy=σy0+Q[1−exp(−bεp)]。断裂能字段 fracture_energy_n_mm 的实际单位为 N/mm，即单位裂面面积的能量；损伤演化使用初始轴向单元特征长度进行正则化。失效单元退出传力。

整体工程应力=端部反力/初始名义截面积，工程应变=端部伸长/初始长度；局部场为对应模型求得的应力，颈缩后不能用整体工程应力替代颈部应力。

## 压缩

圆截面复用轴对称大变形实体单元，矩形使用非线性杆单元。端部施加负位移，显示负工程应变与负工程应力。关闭拉伸损伤；横向自由膨胀。采用无摩擦端面条件，不预测摩擦鼓肚、接触或长杆侧向失稳。标准缩短25%；长杆失稳使用独立模块。

## 扭转

Saint-Venant 扭转杆有限元，左端固定转角、右端受扭矩。圆形采用圆截面 J，矩形采用矩形级数 J 和对应剪应力关系；节点转角来自扭转刚度方程。可视化将计算的截面转角投影成空间扭转形状。矩形存在翘曲，画面仅示意截面转动，不宣称完整三维翘曲位移解。

显示扭矩—转角曲线与剪应力场。加载在首屈服或预定小变形上限停止，不模拟扭转塑性断裂。

## 弯曲

简支梁跨中集中载荷，Euler–Bernoulli 梁单元，节点挠度和转角由整体刚度方程求解。截面正应力依据弯矩与截面惯性矩恢复，画面网格是恢复场载体，不是二维实体有限元单元。忽略剪切变形，适用于细长梁，手动输入的短厚梁仍受这一假设限制。

显示载荷—跨中挠度，加载在首屈服或小挠度上限停止。弯曲惯性矩按矩形高度方向计算；交换宽和高会改变响应。

## 剪切

采用一维均匀直接剪切单元，由剪切能½∫GA(v′)²dx组装刚度，τ=Gγ，G=E/[2(1+ν)]，F=τA，横向相对位移=γL。试样两端施加相反横向作用并约束刚体运动，画面为均匀剪切运动学。不是接触刀具剪断或完整实体有限元，不计算非均匀剪切破坏。显示剪应力—剪应变，至弹性首屈服上限停止。

均匀剪切还假定侧面的配套剪切牵引或运动约束；该图不能理解为自由梁的端部横力试验。

## 压杆失稳

两端铰支梁柱，采用弯曲刚度与几何刚度的广义特征值计算临界载荷和模态。圆形任一横向轴等价，矩形取弱轴。初始侧弯缺陷按长度比例设置；临界前侧向挠度由受压梁柱方程计算。

曲线为轴压力—侧向挠度，并报告有限元临界载荷。标准过程在0.98倍临界载荷、首次截面屈服或小侧挠上限中的最先达到者停止。存在缺陷时实际显示的停止载荷可能低于理想临界值。本模块不预测屈曲后的极限承载力或塑性塌落。

## 数据与回放

CSV 使用 UTF-8 BOM。NPZ 使用不含 pickle 的压缩数组与 JSON 元数据，保存每步网格、场量和状态；兼容首版圆棒拉伸记录。显示放大倍数不改原始数据；曲线的显示单位换算保存在诊断项。计算取消或数值中止时保留已完成步，并在记录中标识状态。

## 方法参考

- FEniCS Numerical Tours, Eulerian buckling of a beam：梁刚度、几何刚度及广义特征值问题。https://comet-fenics.readthedocs.io/en/latest/demo/beam_buckling/beam_buckling.html
- Abaqus, Beam section behavior：矩形扭转常数与 Prandtl 应力函数方法。https://abaqusdocs.eait.uq.edu.au/v6.11/books/usb/pt06ch28s03alm11.html
- Abaqus, Eigenvalue buckling prediction：线性特征值屈曲预测的适用假设。https://docs.software.vt.edu/abaqusv2025/English/SIMACAETHERefMap/simathe-c-eigenbuckling.htm

程序采用自编模型，未调用上述商业软件。计算用于概念展示，不作为实际构件设计或材料试验替代品。
