# 2.1 混合架构改造

Python 保留参数配置、实验调度、Qt 界面、存储导出和数值求解编排。C++ 通过版本化 C ABI 与 ctypes 接口接入，不绑定某个 Python 小版本，也不跨边界传递 C++ 对象。NumPy 持有连续双精度缓冲区，C++ 原位计算，ctypes 调用期间释放 GIL。

本轮已迁移：矩形轴向杆单元显式时间步、Voce 弹塑性与损伤更新；扭转杆刚度组装；梁弹性刚度及几何刚度组装。线性方程和特征值仍由 SciPy 求解。圆截面轴对称大变形求解继续使用已验证的 Numba；均匀剪切保留 NumPy。本轮没有增加混凝土或土力学实验。

`native/core.cpp` 为核心，`tensile_lab/native_backend.py` 为边界，`native/build.py` 编译 Windows DLL。设置环境变量 `MECHANICS_BACKEND=cpp` 强制原生模块，缺失时明确报错；`python` 用于原算法回归，`auto` 优先原生、缺失时回退。结果 diagnostics.compute_backend 记录计算实现。缓存已更换版本前缀，历史文件仍可读取。

当前小模型已有 Numba 编译加速，本轮测量显示矩形轴向的 C++ 尚未超过 Numba；不能据此宣称整机加速。后续应按大型模型性能剖析推进稀疏组装、并行本构和求解器优化。基准报告包含预热后的单次墙钟耗时，主要用来发现回退，不是统计性能结论。

## 动画

写实示意：每类实验采用自身标准圆试样的真实长径比（拉伸4、压缩2、扭转12、弯曲24、剪切2、压杆失稳60），长度与直径使用同一缩放比例。压杆为细长杆、压缩为短粗试样，不再共用相同粗细外形。画面标注动画参考尺寸。同类实验修改手动尺寸、截面或材料时仍复用该类动画，实际配置由曲线、读数和网格云图展示。播放进度驱动金属表面、颈缩、裂纹、褶皱等视觉细节。它是程序绘制的 2.5D 教学示意，不是 AI 视频或裂纹预测。画面明确标注；即使计算仅到弹性阶段，共用动画也不代表该次计算已发生破坏。

网格云图：保留原计算网格、真实位移/场值、色标及放大倍率。两种风格可随时切换，曲线、读数、记录和导出均不因风格变化而改变。未来可在不改变数值结果协议的前提下升级三维渲染。

## 安装

双击 release-v2.1/install.cmd，选择父目录，程序安装到其 TensileLab 子目录。取消选择不会安装。命令行可用 `install.ps1 -Quiet -InstallDirectory 'D:\Apps\MechanicsLab'` 指定完整路径。

自定义目录必须为空或已有本程序的安装标记；不会覆盖无关目录。升级沿用桌面/开始菜单入口。卸载从安装脚本自身目录读取文件清单，只删除清单文件并清理空文件夹；用户自行保存的实验文件保留。旧安装仍存在时，其卸载程序不会被本轮自动执行。

## 开发复现与验收

使用 Python 3.12 创建 .venv-dev，从 offline_packages 安装 requirements-lock.txt，再安装 requirements-build.txt。运行 native/build.py 后运行 main.py。

- tests/verify_native.py：16组两种后端全记录比较及多种网格组装比较。
- tests/verify_visual_styles.py：6实验×2风格×2分辨率×3进度，保存截图。
- main.py --self-test <报告路径>：DLL ABI、24份样本、12组重算、结果往返和导出。
- tests/verify_structural.py：独立理论解、边界值、取消与失稳限制。

最终发布物为 release-v2.1 和当前版本 ZIP。dist-v2.1 / build 为可再生中间产物，完成验证后可清理。当前工作目录不保留旧发行包和历史源码副本；标准样本保持24份。
