# 2.0.0 验证摘要

验证日期：2026-09-09。桌面与发行测试环境：Windows 11 x64、CPython 3.12。

- 24份标准记录：六类实验×两种截面×两种材料，与预设配置逐项匹配并可正常回放。
- 12组独立重新计算：每类实验×圆/矩形，检查对应响应、CSV行数、PNG导出和NPZ数组精确往返。
- 初版圆棒拉伸回归：逐帧反力与历史验收计算一致，保留原有颈缩和中央断裂。
- 圆/矩形25%压缩：与独立有限变形单轴解析解比较，工程应力误差小于0.001%，压缩符号、横向膨胀及无损伤检查通过。
- 扭转、弯曲、剪切、失稳：16组材料/截面组合解析对照通过；20单元Euler临界载荷最大相对误差约8.45×10⁻⁷。
- 矩形扭转：另以二维Prandtl泊松差分解交叉核对，扭转常数J误差约0.023%，最大剪应力系数误差小于0.017%。
- 界面：1280×720及1920×1080截图检查；手动尺寸计算、标准参数恢复、完整回放导出、异实验记录导入、取消计算均通过。
- 冻结发行程序和安装后的程序分别通过24份回放、12组计算及导出/往返检查；桌面快捷方式启动正常。

这些误差是对选用的理想模型和解析解的数值比较，不是对实测材料或真实破坏精度的声明。教学模型假设见“模型与数据说明.md”。

复现基本检查：

```powershell
.\.venv\Scripts\python.exe main.py --self-test verification-output/selftest/report.json
.\.venv\Scripts\python.exe tests/verify_structural.py
.\.venv\Scripts\python.exe tests/review_structural_physics.py
```

运行结果保存在本地verification-output，不纳入仓库。
