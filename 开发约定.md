# 当前 2.1 混合架构版

Python 平台层调用 native/core.cpp 的版本化 C ABI；矩形轴向时间步及结构矩阵组装已迁入 C++，其余继续使用原数值后端。新增和修改内核须通过 tests/verify_native.py 对照。开发环境为 .venv-dev，构建发布到 release-v2.1。细节见改造说明-v2.1.md。

两种显示风格并存：网格云图继续要求全部位移和场量来自计算；写实示意按用户要求为同类实验共用动画，必须标注裂纹/褶皱为视觉示意，不能改变计算记录。

六类材料力学离线桌面实验，Python + PySide6。

engine.py为统一入口，models.py定义共享参数，presets.py维护Fe/Al和24组标准尺寸组合。tension/compression按截面交由axial_solver.py分发；圆棒非线性实体核心在solver.py；torsion/bending/shear/buckling在structural_solver.py。

所有数值必须来自对应计算模型。界面变形可放大且必须标明。圆形与矩形的几何和惯性矩不可混用；矩形非线性杆/结构梁恢复场不得标成三维实体应力。

storage.py保存带格式版本的NPZ（不含pickle），兼容首版记录；export.py按实验物理量输出CSV/PNG/JSON。GUI中材料仅Fe/Al。新增数据字段需同步记录单位和物理含义。

修改计算后须使24份examples的配置与presets逐项一致，并重算受影响样本。main.py --self-test <JSON路径>检查12组计算、24组回放和导出；发行前需对冻结程序和本机安装EXE再执行。

工作目录只保留当前版本；离线依赖在offline_packages，有效开发环境为.venv-dev。安装支持用户选择目录，默认沿用已注册位置，桌面继续使用“力学虚拟实验室”入口。build和dist-v2.1属于可再生构建中间目录，不作为交付物保留。
